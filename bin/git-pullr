#!/bin/bash

branch=$(git rev-parse --abbrev-ref HEAD)
remote=$(git config branch.${branch}.remote)

if [[ $remote == "." ]]; then
  git checkout $(git rev-parse --abbrev-ref @{upstream})
  git pullr 
  git checkout ${branch}
fi

git pull --rebase
